# Copy this file to `.env` and adjust values as needed.
# backend/docker-compose.yml로 띄운 로컬 MySQL을 그대로 사용한다면 기본값을 바꾸지 않아도 됩니다.

# --- DB 연결 (SQLAlchemy URL) ---
# 비밀번호를 코드에 하드코딩하지 않기 위해 반드시 환경 변수로 주입합니다.
COLLECTOR_DB_URL=mysql+pymysql://root:password@localhost:3306/liverpool_news
